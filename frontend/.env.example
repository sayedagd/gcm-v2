# Production (Vercel frontend -> Vercel backend)
VITE_API_BASE_URL=https://your-backend-project.vercel.app

# Local dev proxy target (vite dev server -> local backend)
VITE_PROXY_TARGET=http://127.0.0.1:8080
