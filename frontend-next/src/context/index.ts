import { useAppStore } from '@/store';

/**
 * [AR] سياق النظام المعتمد على Zustand (مع دعم التصفية التلقائية)
 * [EN] Zustand-based System Context (with auto-filtering compatibility)
 */
export const useStore = useAppStore;

